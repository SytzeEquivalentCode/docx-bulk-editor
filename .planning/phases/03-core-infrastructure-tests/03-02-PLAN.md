---
phase: 03-core-infrastructure-tests
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/unit/test_config.py
  - tests/unit/test_backup.py
  - tests/unit/test_logger.py
  - tests/unit/test_db_manager.py
autonomous: true

must_haves:
  truths:
    - "ConfigManager handles corrupt JSON with clear error message"
    - "BackupManager reports specific error when source file is missing"
    - "Logger handles rotation boundary without data loss"
    - "DatabaseManager reports constraint violations clearly"
  artifacts:
    - path: "tests/unit/test_config.py"
      provides: "ConfigManager error condition tests"
      contains: "test_corrupt_json_error_message"
    - path: "tests/unit/test_backup.py"
      provides: "BackupManager error tests"
      contains: "test_source_file_missing_error"
    - path: "tests/unit/test_db_manager.py"
      provides: "DatabaseManager constraint tests"
      contains: "test_unique_constraint_violation_message"
  key_links:
    - from: "tests/unit/test_config.py"
      to: "src/core/config.py"
      via: "error handling paths"
      pattern: "JSONDecodeError|FileNotFoundError"
---

<objective>
Add error condition tests identified in CONTEXT.md to improve error path coverage.

Purpose: CONTEXT.md specifies error conditions that should be tested but are missing or incomplete in current tests. Adding these ensures infrastructure components report errors clearly.

Output: 4-6 new tests covering error conditions for ConfigManager, BackupManager, Logger, and DatabaseManager.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-infrastructure-tests/03-CONTEXT.md
@.planning/phases/03-core-infrastructure-tests/03-RESEARCH.md
@tests/unit/test_config.py
@tests/unit/test_backup.py
@tests/unit/test_logger.py
@tests/unit/test_db_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ConfigManager error condition tests</name>
  <files>tests/unit/test_config.py</files>
  <action>
Add tests to TestConfigManager class for error conditions from CONTEXT.md:

1. **test_corrupt_json_error_message**: Verify JSONDecodeError includes helpful context (filename, line number) - the existing test_invalid_json only checks that exception is raised, not the message quality.

2. **test_type_coercion_safety**: Test that setting a nested key doesn't silently overwrite non-dict values. Example: if config has `{"key": "string"}`, calling `set("key.nested", "value")` should either raise an error or handle gracefully.

Follow existing test patterns:
- Use `@pytest.mark.unit` decorator
- Use `tmp_path` fixture for isolation
- Clear docstrings explaining behavior
- Explicit UTF-8 encoding for file writes
  </action>
  <verify>Run `python -m pytest tests/unit/test_config.py -v -k "error_message or type_coercion"` and confirm new tests pass</verify>
  <done>2 new ConfigManager error tests added and passing</done>
</task>

<task type="auto">
  <name>Task 2: Add BackupManager and DatabaseManager error tests</name>
  <files>tests/unit/test_backup.py, tests/unit/test_db_manager.py</files>
  <action>
Add tests for error conditions:

**BackupManager (tests/unit/test_backup.py):**
In TestBackupEdgeCases class, add:
1. **test_source_file_missing_returns_none**: Verify create_backup returns None (not raises) when source doesn't exist - this is already tested but verify error is logged or can be detected.

**DatabaseManager (tests/unit/test_db_manager.py):**
In TestConnectionManager class, add:
1. **test_unique_constraint_violation_message**: Verify IntegrityError is raised with helpful message when unique constraint violated (settings.key is unique).

2. **test_foreign_key_violation_message**: Verify IntegrityError for invalid foreign key reference includes table name.

Follow existing patterns:
- Use `@pytest.mark.unit` decorator
- Class-based organization
- Clear assertions with helpful failure messages
  </action>
  <verify>Run `python -m pytest tests/unit/test_backup.py tests/unit/test_db_manager.py -v -k "constraint or violation or missing"` and confirm new tests pass</verify>
  <done>2-3 new error condition tests added and passing</done>
</task>

</tasks>

<verification>
1. New tests follow existing patterns (class-based, markers, tmp_path)
2. All new tests pass in isolation
3. Full infrastructure suite still passes (85+ tests now)
4. No regressions
</verification>

<success_criteria>
- [ ] ConfigManager error tests added (2 tests)
- [ ] DatabaseManager constraint violation tests added (2 tests)
- [ ] All infrastructure tests pass (87+ total)
- [ ] Error conditions from CONTEXT.md are covered
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-infrastructure-tests/03-02-SUMMARY.md`
</output>
