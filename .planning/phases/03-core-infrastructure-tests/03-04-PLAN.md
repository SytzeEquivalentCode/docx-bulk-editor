---
phase: 03-core-infrastructure-tests
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "ConfigManager loads defaults, saves configurations, and handles missing files correctly"
    - "BackupManager creates backups before processing, restores after failures, and cleans up old backups per retention policy"
    - "Logger rotates files correctly and uses UTF-8 encoding on Windows"
    - "DatabaseManager performs CRUD operations, handles transactions, and applies schema migrations correctly"
  artifacts:
    - path: "tests/unit/test_config.py"
      provides: "ConfigManager test coverage"
      min_lines: 200
    - path: "tests/unit/test_backup.py"
      provides: "BackupManager test coverage"
      min_lines: 700
    - path: "tests/unit/test_logger.py"
      provides: "Logger test coverage"
      min_lines: 250
    - path: "tests/unit/test_db_manager.py"
      provides: "DatabaseManager test coverage"
      min_lines: 450
  key_links:
    - from: "tests/unit/test_config.py"
      to: "src/core/config.py"
      via: "imports and function calls"
      pattern: "from src.core.config import"
    - from: "tests/unit/test_backup.py"
      to: "src/core/backup.py"
      via: "imports and function calls"
      pattern: "from src.core.backup import"
---

<objective>
Verify Phase 3 success criteria are met with checkpoint for human verification.

Purpose: Final verification that all four infrastructure components (ConfigManager, BackupManager, Logger, DatabaseManager) have comprehensive test coverage meeting the success criteria from ROADMAP.md.

Output: Verification report confirming all success criteria pass, or identification of gaps requiring additional work.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-infrastructure-tests/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full infrastructure test suite with coverage</name>
  <files>tests/unit/test_config.py, tests/unit/test_backup.py, tests/unit/test_logger.py, tests/unit/test_db_manager.py</files>
  <action>
Run infrastructure tests with coverage reporting:

```bash
python -m pytest tests/unit/test_config.py tests/unit/test_backup.py tests/unit/test_logger.py tests/unit/test_db_manager.py --cov=src/core --cov=src/database --cov-report=term-missing -v
```

Capture:
1. Total test count (should be 87+ after gap filling)
2. Pass/fail count
3. Coverage percentages for:
   - src/core/config.py
   - src/core/backup.py
   - src/core/logger.py
   - src/database/db_manager.py

Generate test count by module:
```bash
python -m pytest tests/unit/test_config.py --collect-only -q | tail -1
python -m pytest tests/unit/test_backup.py --collect-only -q | tail -1
python -m pytest tests/unit/test_logger.py --collect-only -q | tail -1
python -m pytest tests/unit/test_db_manager.py --collect-only -q | tail -1
```
  </action>
  <verify>All tests pass, coverage reports generated</verify>
  <done>Test results and coverage report captured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete infrastructure test suite for Phase 3:
- ConfigManager tests (load, save, defaults, missing files, error handling)
- BackupManager tests (create, restore, cleanup, validation, edge cases)
- Logger tests (setup, rotation, UTF-8 encoding, config integration)
- DatabaseManager tests (initialization, CRUD, transactions, constraints)
  </what-built>
  <how-to-verify>
1. Review test count by module:
   - test_config.py: ~13 tests
   - test_backup.py: ~36 tests
   - test_logger.py: ~14 tests
   - test_db_manager.py: ~22 tests
   - Total: 85+ tests

2. Verify coverage meets threshold:
   - config.py: 80%+ coverage
   - backup.py: 80%+ coverage
   - logger.py: 80%+ coverage
   - db_manager.py: 80%+ coverage

3. Verify success criteria from ROADMAP.md Phase 3:
   - ConfigManager: loads defaults, saves configurations, handles missing files
   - BackupManager: creates backups, restores after failures, cleans up old backups
   - Logger: rotates files correctly, uses UTF-8 encoding
   - DatabaseManager: CRUD operations, transactions, schema migrations

4. Run a quick sanity check:
   ```bash
   python -m pytest tests/unit/test_config.py tests/unit/test_backup.py tests/unit/test_logger.py tests/unit/test_db_manager.py -v --tb=short
   ```
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or describe any gaps found</resume-signal>
</task>

</tasks>

<verification>
1. All infrastructure tests pass (85+ tests)
2. Coverage meets 80% threshold for all modules
3. Success criteria from ROADMAP.md verified
4. Human approval received
</verification>

<success_criteria>
- [ ] All infrastructure tests pass
- [ ] Coverage meets 80% for config.py, backup.py, logger.py, db_manager.py
- [ ] Phase 3 success criteria verified
- [ ] Human approval received
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-infrastructure-tests/03-04-SUMMARY.md`
</output>
