---
phase: 03-core-infrastructure-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_db_manager.py
autonomous: true

must_haves:
  truths:
    - "All 85 existing infrastructure tests pass"
    - "Schema validation test reflects current database schema"
  artifacts:
    - path: "tests/unit/test_db_manager.py"
      provides: "DatabaseManager tests with correct schema expectations"
      contains: "duration_seconds"
  key_links:
    - from: "tests/unit/test_db_manager.py"
      to: "src/database/db_manager.py"
      via: "schema column definitions"
      pattern: "expected_columns.*duration_seconds"
---

<objective>
Fix the failing schema validation test to reflect current database schema.

Purpose: The database schema evolved (added `duration_seconds` column to jobs table) but the test expectation was not updated. This fix aligns test expectations with the actual implementation.

Output: All 85 infrastructure tests pass (84 currently pass, 1 fails due to schema drift).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-infrastructure-tests/03-CONTEXT.md
@.planning/phases/03-core-infrastructure-tests/03-RESEARCH.md

# Current failing test output shows schema includes duration_seconds column
# The test expects columns without duration_seconds but implementation has it
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix schema validation test for jobs table</name>
  <files>tests/unit/test_db_manager.py</files>
  <action>
Update test_schema_validation_jobs_table to include the `duration_seconds` column in expected_columns.

Current expected_columns is missing:
```python
"duration_seconds": "REAL"
```

Find the test around line 51-77 and add "duration_seconds": "REAL" to the expected_columns dict, maintaining alphabetical order with the other columns.

The actual schema (from db_manager.py) includes this column, but the test expectation was not updated when the column was added.
  </action>
  <verify>Run `python -m pytest tests/unit/test_db_manager.py::TestDatabaseInitialization::test_schema_validation_jobs_table -v` and confirm it passes</verify>
  <done>test_schema_validation_jobs_table passes with correct schema expectation</done>
</task>

<task type="auto">
  <name>Task 2: Verify all infrastructure tests pass</name>
  <files>tests/unit/test_config.py, tests/unit/test_backup.py, tests/unit/test_logger.py, tests/unit/test_db_manager.py</files>
  <action>
Run the full infrastructure test suite to confirm all 85 tests pass after the schema fix.

Command: `python -m pytest tests/unit/test_config.py tests/unit/test_backup.py tests/unit/test_logger.py tests/unit/test_db_manager.py -v`

Expected: 85 tests pass, 0 failures.
  </action>
  <verify>All 85 tests pass with no failures or errors</verify>
  <done>Full infrastructure test suite passes (85/85 tests)</done>
</task>

</tasks>

<verification>
1. Run failing test in isolation - must pass
2. Run all infrastructure tests - all 85 must pass
3. No regressions in other test files
</verification>

<success_criteria>
- [ ] test_schema_validation_jobs_table passes
- [ ] All 85 infrastructure tests pass
- [ ] No new test failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-infrastructure-tests/03-01-SUMMARY.md`
</output>
