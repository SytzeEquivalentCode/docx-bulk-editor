---
phase: 03-core-infrastructure-tests
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/unit/test_config.py
  - tests/unit/test_backup.py
  - tests/unit/test_logger.py
  - tests/unit/test_db_manager.py
autonomous: true

must_haves:
  truths:
    - "All test files have @pytest.mark.unit markers on test classes"
    - "Test organization follows Phase 2 patterns (class-based)"
    - "Coverage meets 80%+ threshold for infrastructure modules"
  artifacts:
    - path: "tests/unit/test_config.py"
      provides: "ConfigManager tests with markers"
      contains: "@pytest.mark.unit"
    - path: "tests/unit/test_backup.py"
      provides: "BackupManager tests with markers"
      contains: "@pytest.mark.unit"
    - path: "tests/unit/test_logger.py"
      provides: "Logger tests with markers"
      contains: "@pytest.mark.unit"
    - path: "tests/unit/test_db_manager.py"
      provides: "DatabaseManager tests with markers"
      contains: "@pytest.mark.unit"
  key_links:
    - from: "pytest.ini"
      to: "tests/unit/*.py"
      via: "marker configuration"
      pattern: "markers.*unit"
---

<objective>
Add pytest markers to infrastructure tests for consistency with Phase 2 patterns.

Purpose: Phase 2 established the pattern of using @pytest.mark.unit markers on test classes. Infrastructure tests predate this convention and need markers added for consistent test filtering.

Output: All infrastructure test classes have @pytest.mark.unit markers, enabling `pytest -m unit` filtering.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-infrastructure-tests/03-CONTEXT.md
@tests/unit/test_config.py
@tests/unit/test_backup.py
@tests/unit/test_logger.py
@tests/unit/test_db_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add markers to test_config.py and test_backup.py</name>
  <files>tests/unit/test_config.py, tests/unit/test_backup.py</files>
  <action>
Add @pytest.mark.unit decorator to test classes:

**test_config.py:**
- Add `@pytest.mark.unit` before `class TestConfigManager:`
- Add `@pytest.mark.unit` before `class TestConfigManagerIntegration:` (or mark as `@pytest.mark.integration` if preferred)

**test_backup.py:**
- Add `@pytest.mark.unit` before each test class:
  - TestBackupManagerInitialization
  - TestBackupCreation
  - TestBackupValidation
  - TestBackupRestoration
  - TestBackupCleanup
  - TestBackupStorageMetrics
  - TestBackupEdgeCases

Ensure `import pytest` is at top of file (already present in both).

Pattern to follow (from Phase 2 processor tests):
```python
@pytest.mark.unit
class TestClassName:
    """Test suite docstring."""

    def test_example(self):
        ...
```
  </action>
  <verify>Run `python -m pytest tests/unit/test_config.py tests/unit/test_backup.py -m unit --collect-only -q` and verify all tests are collected</verify>
  <done>test_config.py and test_backup.py have @pytest.mark.unit markers on all test classes</done>
</task>

<task type="auto">
  <name>Task 2: Add markers to test_logger.py and test_db_manager.py</name>
  <files>tests/unit/test_logger.py, tests/unit/test_db_manager.py</files>
  <action>
Add @pytest.mark.unit decorator to test classes:

**test_logger.py:**
- Add `@pytest.mark.unit` before `class TestLoggerSetup:`
- Add `@pytest.mark.unit` before `class TestLoggerIntegration:` (or mark as `@pytest.mark.integration`)

**test_db_manager.py:**
- Add `@pytest.mark.unit` before each test class:
  - TestDatabaseInitialization
  - TestDatabaseIndexes
  - TestForeignKeys
  - TestConnectionManager
  - TestIDGeneration
  - TestThreadSafety
  - TestSettingsMethods

Ensure `import pytest` is at top of file (already present in both).
  </action>
  <verify>Run `python -m pytest tests/unit/test_logger.py tests/unit/test_db_manager.py -m unit --collect-only -q` and verify all tests are collected</verify>
  <done>test_logger.py and test_db_manager.py have @pytest.mark.unit markers on all test classes</done>
</task>

<task type="auto">
  <name>Task 3: Verify marker filtering works across all infrastructure tests</name>
  <files>tests/unit/test_config.py, tests/unit/test_backup.py, tests/unit/test_logger.py, tests/unit/test_db_manager.py</files>
  <action>
Run marker-filtered test collection and execution:

1. Collect tests with unit marker:
   `python -m pytest tests/unit/test_config.py tests/unit/test_backup.py tests/unit/test_logger.py tests/unit/test_db_manager.py -m unit --collect-only -q`

   Expected: 85+ tests collected

2. Run tests with unit marker:
   `python -m pytest tests/unit/test_config.py tests/unit/test_backup.py tests/unit/test_logger.py tests/unit/test_db_manager.py -m unit -v`

   Expected: All tests pass
  </action>
  <verify>Marker filtering collects and runs all infrastructure tests successfully</verify>
  <done>All infrastructure tests can be filtered with `pytest -m unit` and all pass</done>
</task>

</tasks>

<verification>
1. `pytest -m unit` collects all infrastructure tests (85+)
2. All tests still pass after adding markers
3. Markers are on classes, not individual methods (following Phase 2 pattern)
</verification>

<success_criteria>
- [ ] All test classes in 4 infrastructure test files have @pytest.mark.unit
- [ ] `pytest -m unit` collects all infrastructure tests
- [ ] All tests pass with marker filtering
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-infrastructure-tests/03-03-SUMMARY.md`
</output>
