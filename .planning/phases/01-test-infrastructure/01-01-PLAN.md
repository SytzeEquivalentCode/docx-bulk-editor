---
phase: 01-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - tests/unit/test_fixtures.py
autonomous: true

must_haves:
  truths:
    - "pytest collects all 288 tests without import errors"
    - "docx_factory fixture creates valid .docx files that python-docx can read"
    - "test_db fixture provides isolated in-memory database with schema initialized"
    - "Sample fixtures (sample_docx, complex_docx) produce correct document structures"
  artifacts:
    - path: "tests/unit/test_fixtures.py"
      provides: "Validation tests for core fixtures"
      min_lines: 80
    - path: ".planning/STATE.md"
      provides: "Updated project state with resolved blocker"
      contains: "blocker resolved"
  key_links:
    - from: "tests/unit/test_fixtures.py"
      to: "tests/conftest.py"
      via: "fixture imports"
      pattern: "def test_.*\\(docx_factory|test_db|sample_docx"
---

<objective>
Validate existing test infrastructure meets Phase 1 requirements and update project documentation.

Purpose: Research revealed infrastructure already exists. This plan verifies it works correctly in practice and updates STATE.md to remove the stale blocker about import errors.

Output: Validation test file proving fixtures work + updated STATE.md reflecting actual project state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-RESEARCH.md
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture validation tests</name>
  <files>tests/unit/test_fixtures.py</files>
  <action>
Create a test file that validates the existing test infrastructure fixtures work correctly.

Tests to include:

1. **docx_factory fixture validation:**
   - Test basic document creation (content only)
   - Test metadata setting (author, title)
   - Test add_heading option produces heading paragraph
   - Test add_table option produces 2x2 table
   - Test returned Path exists and is readable by python-docx
   - Test paragraph splitting on newlines works correctly

2. **test_db fixture validation:**
   - Test database is initialized with schema (jobs table exists)
   - Test basic CRUD: can insert and retrieve a setting
   - Test isolation: changes don't persist across tests (use two tests that write/read same key)

3. **Pre-made fixture validation:**
   - Test sample_docx has expected structure (heading + 3 paragraphs)
   - Test complex_docx has expected structure (heading + paragraphs + table)
   - Test metadata is set correctly on pre-made fixtures

4. **Helper function validation:**
   - Test assert_docx_contains_text finds existing text
   - Test assert_docx_contains_text raises AssertionError for missing text
   - Test assert_docx_property validates metadata correctly

Mark all tests with `@pytest.mark.unit` decorator.

Use this structure:
```python
"""Validation tests for test infrastructure fixtures.

These tests verify the fixtures in conftest.py work correctly,
serving as both validation and documentation of fixture behavior.
"""

import pytest
from pathlib import Path
from docx import Document

# Import helper functions from conftest
from conftest import assert_docx_contains_text, assert_docx_property


@pytest.mark.unit
class TestDocxFactory:
    """Tests for docx_factory fixture."""
    ...

@pytest.mark.unit
class TestTestDb:
    """Tests for test_db fixture."""
    ...

@pytest.mark.unit
class TestPreMadeFixtures:
    """Tests for sample_docx and complex_docx fixtures."""
    ...

@pytest.mark.unit
class TestHelperFunctions:
    """Tests for assertion helper functions."""
    ...
```
  </action>
  <verify>
Run: `pytest tests/unit/test_fixtures.py -v`
All tests should pass.
  </verify>
  <done>
Validation test file exists with 12+ tests covering all core fixtures.
All tests pass, proving infrastructure works correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md to remove stale blocker</name>
  <files>.planning/STATE.md</files>
  <action>
Update STATE.md to reflect the actual project state:

1. **Remove the stale blocker** - The blocker about `ModuleNotFoundError` is resolved. Tests collect successfully (288 tests).

2. **Update Blockers/Concerns section:**
   - Change from "Tests currently fail to collect..." to note that this has been resolved
   - Add note: "Blocker resolved: Tests collect successfully (288 tests verified 2026-01-30)"

3. **Update Last activity** to reflect this validation work.

4. **Keep all other content intact** - decisions, pending todos, session continuity.

Do NOT change:
- Current Position section (still Phase 1, Plan 0 until this completes)
- Performance Metrics section
- Accumulated Context > Decisions section
  </action>
  <verify>
Read STATE.md and confirm:
- Blockers/Concerns section no longer shows import error as current blocker
- Note about resolution is present
  </verify>
  <done>
STATE.md accurately reflects project state with resolved blocker documented.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run full test collection to confirm nothing broke:
   `pytest --collect-only 2>&1 | tail -5`
   Should show "288 items" (or close to it, may be slightly more with new test file)

2. Run the new validation tests:
   `pytest tests/unit/test_fixtures.py -v`
   All tests should pass.

3. Verify STATE.md updated:
   `grep -i "blocker" .planning/STATE.md`
   Should show "resolved" language, not active blocker about imports.
</verification>

<success_criteria>
- [ ] tests/unit/test_fixtures.py exists with 12+ validation tests
- [ ] All validation tests pass
- [ ] STATE.md blocker section updated to show resolution
- [ ] Total test count increased (288 + new tests)
- [ ] No regressions in test collection
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-01-SUMMARY.md`
</output>
