---
phase: 01-test-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [pytest.ini, .planning/STATE.md]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "pytest collects all test files without import errors"
    - "test_db fixture imports src.database.db_manager successfully"
    - "test_config fixture imports src.core.config successfully"
  artifacts:
    - path: "pytest.ini"
      provides: "pythonpath configuration for pytest"
      contains: "pythonpath = ."
  key_links:
    - from: "pytest"
      to: "src/"
      via: "pythonpath configuration"
      pattern: "pythonpath\\s*=\\s*\\."
---

<objective>
Fix pytest Python path configuration to enable test collection for all 13 blocked test files.

Purpose: The VERIFICATION.md identified that 13 test files fail to collect because pytest cannot find the `src` module. The root cause is missing `pythonpath` configuration - the module exists and works when Python path is configured manually. This single configuration change unblocks both INFRA-01 (test collection) and INFRA-03 (test_db fixture).

Output: Updated pytest.ini with pythonpath configuration; all 60 tests collect successfully.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure/01-VERIFICATION.md
@pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pythonpath to pytest.ini</name>
  <files>pytest.ini</files>
  <action>
Add the pythonpath configuration to pytest.ini to enable pytest to find the src module.

Add this line after [pytest] section header:
```
pythonpath = .
```

This is the simplest fix - it tells pytest to add the project root to Python's import path, which allows `from src.xxx import yyy` to work.

Do NOT:
- Modify pyproject.toml (pytest.ini is already the primary pytest config)
- Create a root conftest.py (conftest already exists in tests/)
- Install the package in editable mode (unnecessary complexity)
  </action>
  <verify>
1. Verify pythonpath configuration exists in pytest.ini:
   - Run: `grep -E "pythonpath\s*=" pytest.ini`
   - Expected: Line containing "pythonpath = ."

2. Run: `pytest --collect-only 2>&1 | head -50`
   - Expected: "collected 60 items" (or similar, with 0 errors)
   - Previously: "collected 47 items / 13 errors"

3. Verify specific blocked tests now collect:
   - `pytest --collect-only tests/unit/test_db_manager.py`
   - Expected: Shows test items, no ModuleNotFoundError

4. Verify test_db fixture can be used:
   - `pytest tests/unit/test_fixtures.py::TestTestDb -v --collect-only`
   - Expected: Tests listed (may still skip if DatabaseManager has issues, but NO import error)

5. Verify test_config fixture can be used:
   - `pytest tests/unit/test_fixtures.py::TestTestConfig -v --collect-only`
   - Expected: Tests listed (may still skip if ConfigManager has issues, but NO import error)
  </verify>
  <done>
- pytest.ini contains pythonpath = .
- `grep -E "pythonpath\s*=" pytest.ini` returns match
- `pytest --collect-only` shows 0 collection errors
- All 13 previously-blocked test files now collect
- TestTestDb and TestTestConfig fixtures collect without ModuleNotFoundError
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md blocker status</name>
  <files>.planning/STATE.md</files>
  <action>
Update lines 55-74 in .planning/STATE.md to reflect that the pytest import path issue is resolved.

BEFORE (lines 55-74):
```
**Current blockers:**
- 13 test files fail to collect due to import errors from non-existent src/ modules
  - Affected: test_backup.py, test_config.py, test_db_manager.py, test_logger.py, test_metadata.py,
    test_paragraph_replacement.py, test_processor_result.py, test_search_replace_patterns.py,
    test_style_enforcer.py, test_table_header_footer.py, test_validator.py, test_main_window.py,
    test_core_infrastructure.py
  - Root cause: Tests import from src.core.*, src.database.*, src.processors.*, src.ui.*, src.workers.*
  - These modules exist but have incomplete implementations or missing dependencies
  - conftest.py fixtures `test_db` and `test_config` also blocked (import DatabaseManager, ConfigManager)

**Working infrastructure (validated 2026-01-30):**
- 8 tests collect successfully (test_processing_speed.py performance benchmarks)
- 34 fixture validation tests pass (tests/unit/test_fixtures.py)
- Working fixtures: docx_factory, sample_docx, complex_docx, large_docx, multiple_docx,
  temp_workspace, mock_config, unicode_filename, qapp, cleanup_workers
- Helper functions work: assert_docx_contains_text, assert_docx_property

**Resolution path:**
- Phase 2+ will implement src/ modules, which will unblock the 13 failing test files
- No action needed on test infrastructure itself - fixtures are correctly implemented
```

AFTER (replace lines 55-74 with):
```
**[RESOLVED] Import path blocker (2026-01-30):**
- Fixed: Added `pythonpath = .` to pytest.ini
- Result: All 13 previously-blocked test files now COLLECT successfully
- Note: Tests may still SKIP or FAIL due to incomplete src/ module implementations
  - This is expected behavior - collection errors vs test failures are different:
  - Collection error: pytest cannot load test file (blocks test runner)
  - Test failure/skip: pytest loads file, test runs but doesn't pass (normal feedback)
- conftest.py fixtures `test_db` and `test_config` now import successfully

**Working infrastructure (validated 2026-01-30):**
- All 60 tests collect successfully (0 collection errors)
- 34 fixture validation tests pass (tests/unit/test_fixtures.py)
- Working fixtures: docx_factory, sample_docx, complex_docx, large_docx, multiple_docx,
  temp_workspace, mock_config, unicode_filename, qapp, cleanup_workers, test_db, test_config
- Helper functions work: assert_docx_contains_text, assert_docx_property

**Next steps:**
- Phase 2+ will implement src/ modules, which will make currently-skipping tests pass
- Test infrastructure is complete - fixtures work, collection works
```

The key changes:
1. "[RESOLVED]" marker clearly indicates the blocker is fixed
2. Explains the fix (pythonpath = .)
3. Clarifies the difference between collection errors (fixed) and test failures (expected)
4. Updates fixture list to include test_db and test_config
5. Changes "Resolution path" to "Next steps" with accurate status
  </action>
  <verify>
1. Verify [RESOLVED] marker exists:
   - Run: `grep -F "[RESOLVED]" .planning/STATE.md`
   - Expected: Line containing "[RESOLVED] Import path blocker"

2. Verify pythonpath fix is documented:
   - Run: `grep "pythonpath = ." .planning/STATE.md`
   - Expected: Line mentioning the fix

3. Verify collection vs failure distinction is documented:
   - Run: `grep -A2 "collection errors vs test failures" .planning/STATE.md`
   - Expected: Lines explaining the difference

4. Verify test_db and test_config mentioned as working:
   - Run: `grep "test_db, test_config" .planning/STATE.md`
   - Expected: Line in Working fixtures list
  </verify>
  <done>
- STATE.md lines 55-74 updated with [RESOLVED] marker
- Fix documented: "pythonpath = ." mentioned
- Clear distinction between collection errors and test failures
- test_db and test_config listed as working fixtures
  </done>
</task>

</tasks>

<verification>
Run full test collection to confirm gap closure:

```bash
# Verify pythonpath in pytest.ini
grep -E "pythonpath\s*=" pytest.ini

# Full collection should show 0 errors
pytest --collect-only

# Verify previously-blocked files
pytest --collect-only tests/unit/test_db_manager.py tests/unit/test_backup.py tests/unit/test_config.py

# Check if test_db and test_config fixture validation tests run (may skip, but should not error)
pytest tests/unit/test_fixtures.py::TestTestDb -v
pytest tests/unit/test_fixtures.py::TestTestConfig -v

# Verify STATE.md updates
grep -F "[RESOLVED]" .planning/STATE.md
grep "pythonpath = ." .planning/STATE.md
```

Expected outcomes:
- pytest.ini contains pythonpath configuration
- 0 collection errors (was 13)
- ~60 tests collected (was 47)
- TestTestDb and TestTestConfig may skip with "fixture implementation requires working module" but NOT fail with ModuleNotFoundError
- STATE.md contains [RESOLVED] marker and documents the fix
</verification>

<success_criteria>
1. pytest.ini contains `pythonpath = .` configuration
2. `pytest --collect-only` reports 0 collection errors
3. All 13 previously-blocked test files are now collectible
4. TestTestDb and TestTestConfig fixtures collect without import errors
5. STATE.md updated with [RESOLVED] marker and pythonpath fix documented
6. Gap closure verified: INFRA-01 and INFRA-03 now satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-02-SUMMARY.md`
</output>
