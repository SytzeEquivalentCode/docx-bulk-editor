---
phase: 02-processor-tests
plan: 07
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/unit/test_validator.py
autonomous: true

must_haves:
  truths:
    - "Validator tests use programmatic document generation (docx_factory)"
    - "Heading hierarchy validation tests verify correct issue detection"
    - "Placeholder detection tests cover all placeholder patterns"
    - "Whitespace issue tests verify trailing/multiple space detection"
    - "Report formatting tests verify human-readable output"
    - "Error paths tested (invalid file, corrupted file)"
  artifacts:
    - path: "tests/unit/test_validator.py"
      provides: "Comprehensive validator tests"
      min_lines: 300
      contains: "process_document"
  key_links:
    - from: "tests/unit/test_validator.py"
      to: "src/processors/validator.py"
      via: "import statement"
      pattern: "from src.processors.validator import"
---

<objective>
Validate and extend Validator processor tests for PROC-06 compliance

Purpose: Validator processor tests exist (test_validator.py) and are comprehensive. This plan validates they meet phase requirements: programmatic document generation, proper markers, coverage of all validation rules. Minor extensions may be added for any gaps identified.

Output: Validated test_validator.py meeting PROC-06 requirement that "Validator reports all validation rule violations accurately"
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-processor-tests/02-CONTEXT.md
@.planning/phases/02-processor-tests/02-01-SUMMARY.md

@src/processors/validator.py
@tests/conftest.py
@tests/unit/test_validator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate existing test_validator.py meets requirements</name>
  <files>tests/unit/test_validator.py</files>
  <action>
Run the existing validator tests and validate they meet phase requirements:

1. Run tests to verify they all pass:
```bash
pytest tests/unit/test_validator.py -v
```

2. Verify programmatic document generation:
- Check that docx_factory fixture is used (not static files)
- Confirm no shutil.copy or test_docs_dir patterns

3. Verify coverage of validation rules per CONTEXT.md:
- Heading hierarchy validation
- Empty paragraph detection
- Placeholder pattern detection ({{variable}}, [TODO], [TBD], etc.)
- Whitespace issue detection
- Report formatting

4. Verify test markers:
- All tests have @pytest.mark.unit markers

5. Verify error paths:
- Invalid file handling
- Corrupted file handling

Expected: All checks pass. Existing test file is comprehensive (~834 lines, 45+ tests).
  </action>
  <verify>
Run: `pytest tests/unit/test_validator.py -v`
All tests should pass. Expected: ~45 tests passing.

Check for static file deps:
```bash
grep -c "shutil" tests/unit/test_validator.py
grep -c "test_docs_dir" tests/unit/test_validator.py
```
Both should return 0.
  </verify>
  <done>test_validator.py passes all tests and meets phase requirements for programmatic document generation</done>
</task>

<task type="auto">
  <name>Task 2: Add any missing edge case tests</name>
  <files>tests/unit/test_validator.py</files>
  <action>
Review existing tests and add any missing edge cases per CONTEXT.md requirements:

**Check for these specific test cases (add if missing):**

1. Unicode content handling (Chinese text, accented characters)
   - Existing: TestEdgeCases.test_unicode_content (present)

2. Empty document handling
   - Existing: TestEdgeCases.test_empty_document (present)

3. Long documents
   - Existing: TestEdgeCases.test_very_long_document (present)

4. Multiple tables with placeholders
   - Existing: TestEdgeCases.test_many_tables (present)

5. All placeholder types (parametrized)
   - Existing: TestEdgeCases.test_all_placeholder_types (present, parametrized)

**If all edge cases are covered, no changes needed.**

**If any gaps found, add tests following existing patterns:**
```python
@pytest.mark.unit
def test_[scenario](self, docx_factory):
    """[Description of what is tested]."""
    doc_path = docx_factory(content="...")
    # ... test logic
    assert result.success is True
```
  </action>
  <verify>
Run: `pytest tests/unit/test_validator.py -v`
All tests pass including any new additions.
  </verify>
  <done>Validator tests cover all required edge cases per CONTEXT.md</done>
</task>

<task type="auto">
  <name>Task 3: Generate coverage report for validator processor</name>
  <files></files>
  <action>
Generate coverage report to document current state:

```bash
pytest tests/unit/test_validator.py --cov=src/processors/validator --cov-report=term-missing
```

Document coverage percentage and any uncovered lines.

Acceptable coverage: >70% (per CLAUDE.md testing standards).
  </action>
  <verify>
Coverage report generated showing:
- src/processors/validator.py has coverage data
- Coverage >70%
  </verify>
  <done>Coverage report generated documenting validator test coverage</done>
</task>

</tasks>

<verification>
1. `pytest tests/unit/test_validator.py -v` - All tests pass
2. No static file dependencies (grep returns 0)
3. Coverage >70% for src/processors/validator.py
</verification>

<success_criteria>
- test_validator.py exists and all tests pass
- Uses programmatic document generation (docx_factory)
- Has @pytest.mark.unit markers on all tests
- Covers: heading hierarchy, empty paragraphs, placeholders, whitespace
- Covers: report formatting
- Covers: error paths (invalid file, corrupted file)
- Coverage documented
</success_criteria>

<output>
After completion, create `.planning/phases/02-processor-tests/02-07-SUMMARY.md`
</output>
