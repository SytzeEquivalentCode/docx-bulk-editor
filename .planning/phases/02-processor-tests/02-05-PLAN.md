---
phase: 02-processor-tests
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All five processor test files pass pytest execution"
    - "No collection errors across entire test suite"
    - "Test count meets phase goals (100+ processor tests)"
  artifacts:
    - path: "tests/unit/test_search_replace.py"
      provides: "Consolidated search/replace tests"
    - path: "tests/unit/test_metadata.py"
      provides: "Metadata processor tests"
    - path: "tests/unit/test_table_formatter.py"
      provides: "Table formatter tests"
    - path: "tests/unit/test_style_enforcer.py"
      provides: "Style enforcer tests"
    - path: "tests/unit/test_validator.py"
      provides: "Validator tests"
  key_links:
    - from: "tests/unit/"
      to: "src/processors/"
      via: "import statements"
      pattern: "from src.processors"
---

<objective>
Verify all processor tests pass and phase goals are met

Purpose: After implementing plans 01-04, verify that all five processor test suites pass, no collection errors exist, and test coverage meets phase requirements. This is a verification checkpoint before marking the phase complete.

Output: Verification report confirming all processor tests pass
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-processor-tests/02-CONTEXT.md

@.planning/phases/02-processor-tests/02-01-SUMMARY.md
@.planning/phases/02-processor-tests/02-02-SUMMARY.md
@.planning/phases/02-processor-tests/02-03-SUMMARY.md
@.planning/phases/02-processor-tests/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full processor test suite</name>
  <files></files>
  <action>
Run pytest on all processor test files to verify everything passes.

Commands:
```bash
# Run all processor tests with verbose output
pytest tests/unit/test_search_replace.py tests/unit/test_metadata.py tests/unit/test_table_formatter.py tests/unit/test_style_enforcer.py tests/unit/test_validator.py -v

# Count total tests
pytest tests/unit/test_search_replace.py tests/unit/test_metadata.py tests/unit/test_table_formatter.py tests/unit/test_style_enforcer.py tests/unit/test_validator.py --collect-only | grep "test session"

# Check for any collection errors
pytest tests/ --collect-only 2>&1 | grep -i "error"
```

Expected outcomes:
- All tests pass (no failures)
- No collection errors
- Total test count: 100+ tests across all five files
  </action>
  <verify>
Command output shows:
- "X passed" with X >= 100
- No failures
- No collection errors
  </verify>
  <done>All processor tests pass with no collection errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete processor test suite for all five processors:
- Search & Replace (consolidated from 3 files, programmatic docs)
- Metadata (converted to programmatic docs)
- Table Formatter (new comprehensive tests)
- Style Enforcer (existing, verified working)
- Validator (existing, verified working)
  </what-built>
  <how-to-verify>
1. Review pytest output from Task 1
2. Verify test counts meet expectations:
   - test_search_replace.py: ~45 tests
   - test_metadata.py: ~26 tests
   - test_table_formatter.py: ~30 tests
   - test_style_enforcer.py: ~30 tests (existing)
   - test_validator.py: ~45 tests (existing)
   - Total: ~175 tests

3. Verify old redundant test files are removed:
   - tests/unit/test_search_replace_patterns.py (should NOT exist)
   - tests/unit/test_paragraph_replacement.py (should NOT exist)
   - tests/unit/test_table_header_footer.py (should NOT exist)

4. Verify no static file dependencies remain:
   ```bash
   grep -r "shutil.copy" tests/unit/test_*.py
   grep -r "test_docs_dir" tests/unit/test_*.py
   ```
   Both commands should return no results for processor test files.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Generate test coverage summary</name>
  <files></files>
  <action>
Generate coverage report for processor modules to document current state.

```bash
pytest tests/unit/test_search_replace.py tests/unit/test_metadata.py tests/unit/test_table_formatter.py tests/unit/test_style_enforcer.py tests/unit/test_validator.py --cov=src/processors --cov-report=term-missing
```

Document the coverage percentages for each processor module:
- src/processors/search_replace.py
- src/processors/metadata.py
- src/processors/table_formatter.py
- src/processors/style_enforcer.py
- src/processors/validator.py

Note: Some lines may be uncovered due to error handling paths or edge cases not yet exercised. This is acceptable as long as overall coverage is >70%.
  </action>
  <verify>
Coverage report generated showing:
- All processor modules have coverage data
- Overall processor coverage >70%
  </verify>
  <done>Coverage report generated documenting processor test coverage</done>
</task>

</tasks>

<verification>
1. `pytest tests/unit/test_search_replace.py tests/unit/test_metadata.py tests/unit/test_table_formatter.py tests/unit/test_style_enforcer.py tests/unit/test_validator.py -v` - All pass
2. Total test count >= 100
3. No collection errors
4. No static file dependencies (grep returns empty)
5. Coverage report generated
</verification>

<success_criteria>
- All five processor test files execute successfully
- 100+ total processor tests pass
- No collection errors
- Old redundant test files removed
- No static file dependencies remain
- Coverage documented
</success_criteria>

<output>
After completion, create `.planning/phases/02-processor-tests/02-05-SUMMARY.md`
</output>
