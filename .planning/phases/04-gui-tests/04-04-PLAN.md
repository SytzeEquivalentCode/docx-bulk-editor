---
phase: 04-gui-tests
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All GUI tests pass"
    - "GUI-01 through GUI-04 requirements are covered"
    - "No test uses time.sleep()"
    - "All dialogs are mocked"
  artifacts:
    - path: "tests/gui/test_main_window.py"
      provides: "MainWindow tests"
      min_lines: 200
    - path: "tests/gui/test_progress_dialog.py"
      provides: "ProgressDialog tests"
      min_lines: 150
    - path: "tests/gui/test_history_window.py"
      provides: "HistoryWindow tests"
      min_lines: 150
  key_links: []
---

<objective>
Verify Phase 4 success criteria and GUI requirement coverage.

Purpose: Final checkpoint to confirm all GUI tests pass and requirements GUI-01 through GUI-04 are satisfied. Human verification ensures tests actually test user-facing behavior.

Output: Phase 4 marked complete after human approval
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gui-tests/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run complete GUI test suite</name>
  <files></files>
  <action>
Run all GUI tests and capture results:

```bash
pytest tests/gui/ -v --tb=short
```

Capture:
- Total tests count
- Pass/fail status
- Any warnings

If any tests fail, document the failure and stop. DO NOT proceed to checkpoint with failing tests.
  </action>
  <verify>
All tests in tests/gui/ pass.
  </verify>
  <done>
GUI test suite passes completely with X tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify requirement coverage</name>
  <files></files>
  <action>
Check each GUI requirement is covered:

**GUI-01: MainWindow file selection, drag-drop, operation switching**
- Grep for tests covering: _on_add_files, _on_add_folder, _on_clear_files, dragEnterEvent, dropEvent, _on_operation_selected
- List test names that cover each

**GUI-02: MainWindow start/cancel job flow**
- Grep for tests covering: _on_start_clicked, worker, cancel, validation warnings
- List test names that cover each

**GUI-03: ProgressDialog updates, elapsed time, cancellation**
- Grep for tests covering: update_progress, _format_time, cancelled signal, cancel_button
- List test names that cover each

**GUI-04: HistoryWindow listing, filtering, result viewing**
- Grep for tests covering: jobs_table, operation_filter, status_filter, details_text, _on_export
- List test names that cover each

Format as coverage matrix:
```
| Requirement | Aspect | Test Name(s) | Status |
|-------------|--------|--------------|--------|
| GUI-01 | File dialog | test_add_files_via_dialog | Covered |
| ... | ... | ... | ... |
```
  </action>
  <verify>
Coverage matrix shows all GUI requirements have at least one test per aspect.
  </verify>
  <done>
Coverage matrix confirms GUI-01 through GUI-04 are fully covered.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify test quality patterns</name>
  <files></files>
  <action>
Check for anti-patterns in test code:

1. **No time.sleep():**
```bash
grep -r "time.sleep" tests/gui/
```
Should return nothing. If found, list files.

2. **All tests have @pytest.mark.gui:**
```bash
grep -L "@pytest.mark.gui" tests/gui/test_*.py
```
Should return nothing (all files have marker).

3. **No real dialog interactions:**
```bash
grep -r "exec()" tests/gui/ | grep -v "monkeypatch"
```
Should return nothing (dialogs are mocked).

4. **All widgets registered with qtbot:**
```bash
grep -c "qtbot.addWidget" tests/gui/test_*.py
```
Count should be >= number of test methods creating widgets.

Report findings.
  </action>
  <verify>
No anti-patterns found. All tests follow pytest-qt best practices.
  </verify>
  <done>
Quality audit passed: no sleeps, all markers present, dialogs mocked, widgets registered.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete GUI test suite covering MainWindow, ProgressDialog, and HistoryWindow</what-built>
  <how-to-verify>
Review the test summary and coverage matrix presented.

Verify:
1. All 4 GUI requirements (GUI-01 through GUI-04) have tests
2. Test counts are reasonable (~50+ total GUI tests)
3. No obvious gaps in coverage

Optional: Run the tests yourself:
```bash
cd C:\Temp\Dev\docx-bulk-editor
.\venv\Scripts\activate
pytest tests/gui/ -v --tb=short
```
  </how-to-verify>
  <resume-signal>Type "approved" if phase complete, or describe any concerns</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when:
1. All GUI tests pass
2. GUI-01 through GUI-04 requirements covered
3. Test quality audit passes
4. Human approves the test coverage
</verification>

<success_criteria>
- 50+ GUI tests pass
- No test failures or errors
- Coverage matrix shows all requirement aspects tested
- No anti-patterns (sleeps, real dialogs, missing markers)
- Human approval received
</success_criteria>

<output>
After human approval, create `.planning/phases/04-gui-tests/04-04-SUMMARY.md` with final test counts and coverage confirmation.
</output>
