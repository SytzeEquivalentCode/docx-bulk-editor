---
phase: 05-integration-e2e-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_error_recovery.py
  - tests/integration/test_cancellation.py
autonomous: true

must_haves:
  truths:
    - "Processing continues after individual file failures"
    - "Corrupt files are NOT modified by failed processing"
    - "Valid files in batch succeed even when some fail"
    - "Job marked as partial_success or completed_with_errors when some files fail"
    - "User cancellation stops processing without corrupting documents"
    - "Cancelled jobs are marked as 'cancelled' in database"
  artifacts:
    - path: "tests/integration/test_error_recovery.py"
      provides: "Error recovery and partial failure tests"
      min_lines: 150
    - path: "tests/integration/test_cancellation.py"
      provides: "User cancellation tests"
      min_lines: 80
  key_links:
    - from: "tests/integration/test_error_recovery.py"
      to: "src/workers/job_worker.py"
      via: "failure handling logic"
      pattern: "failure_count"
    - from: "tests/integration/test_cancellation.py"
      to: "src/workers/job_worker.py"
      via: "cancellation flag check"
      pattern: "_cancelled"
---

<objective>
Create error recovery and cancellation tests that validate graceful handling of failures and user-initiated cancellation.

Purpose: Validate E2E-02 requirement - processor failure handling and graceful degradation. Tests verify that the application handles partial failures correctly and allows safe cancellation without data corruption.

Output: `tests/integration/test_error_recovery.py` and `tests/integration/test_cancellation.py` with comprehensive error handling tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-e2e-tests/05-CONTEXT.md
@.planning/phases/05-integration-e2e-tests/05-RESEARCH.md
@tests/conftest.py
@src/ui/main_window.py
@src/workers/job_worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error recovery tests with corrupt documents</name>
  <files>tests/integration/test_error_recovery.py</files>
  <action>
Create `tests/integration/test_error_recovery.py` with the following structure:

1. **Imports and markers:**
   - Import pytest, pathlib, PySide6 components
   - Import MainWindow, Document (python-docx)
   - Apply `@pytest.mark.integration` class marker

2. **Corrupt document fixtures (at module level):**
   ```python
   @pytest.fixture
   def corrupt_invalid_zip(tmp_path):
       """Create file that's not a valid ZIP/DOCX."""
       corrupt = tmp_path / "corrupt_zip.docx"
       corrupt.write_bytes(b"Not a ZIP file at all")
       return corrupt

   @pytest.fixture
   def corrupt_empty(tmp_path):
       """Create empty file."""
       corrupt = tmp_path / "corrupt_empty.docx"
       corrupt.write_bytes(b"")
       return corrupt

   @pytest.fixture
   def corrupt_truncated(tmp_path):
       """Create truncated ZIP that starts valid but is incomplete."""
       corrupt = tmp_path / "corrupt_truncated.docx"
       # ZIP header bytes followed by garbage
       corrupt.write_bytes(b'PK\x03\x04' + b'\x00' * 100)
       return corrupt
   ```

3. **TestPartialFailureHandling class:**
   - `test_processing_continues_after_file_failure`: Core partial failure test
     - Create 8 valid documents with `docx_factory`
     - Add 2 corrupt documents (invalid_zip, empty)
     - Process all 10 files with search_replace operation
     - Verify: 8 files processed successfully
     - Verify: 2 files marked as failed
     - Verify: Job completed (not aborted)
     - Verify: Database has error_count=2 or status='completed_with_errors'

   - `test_corrupt_files_not_modified`: Verify corrupt files unchanged
     - Create corrupt files and record original bytes
     - Process batch including corrupt files
     - Verify: Corrupt file bytes unchanged after processing
     - Verify: No backup created for failed files (or backup matches original)

   - `test_valid_files_modified_despite_failures`: Verify valid files succeed
     - Mix of valid and corrupt files
     - Process batch
     - Verify: Valid files have expected changes (text replaced or metadata updated)
     - Verify: Changes persisted to disk

4. **TestErrorReporting class:**
   - `test_error_details_stored_in_database`: Verify error info captured
     - Process batch with corrupt file
     - Query job_results table for failed entries
     - Verify: error_message field contains meaningful error (not empty)
     - Verify: file_path correctly identifies which file failed

   - `test_job_summary_includes_error_count`: Verify job completion results
     - Process batch with failures
     - Check job_completed signal payload
     - Verify: failure_count matches actual failures
     - Verify: success_count + failure_count = total_files
  </action>
  <verify>Run `pytest tests/integration/test_error_recovery.py -v` - tests should collect and execute</verify>
  <done>test_error_recovery.py exists with corrupt document fixtures and TestPartialFailureHandling, TestErrorReporting classes</done>
</task>

<task type="auto">
  <name>Task 2: Create user cancellation tests</name>
  <files>tests/integration/test_cancellation.py</files>
  <action>
Create `tests/integration/test_cancellation.py` with the following structure:

1. **Imports and markers:**
   - Import pytest, hashlib (for file integrity checks), time
   - Import PySide6 components (Qt, QTest, QApplication)
   - Import MainWindow, Document
   - Apply `@pytest.mark.integration` and `@pytest.mark.timeout(60)` markers

2. **TestUserCancellation class:**
   - `test_cancellation_stops_processing`: Core cancellation test
     - Create 50 test documents (large batch to ensure processing takes time)
     - Start processing
     - Wait briefly (500ms) for processing to start
     - Call worker.cancel()
     - Wait for job_cancelled or job_completed signal
     - Verify: Job status is 'cancelled' in database
     - Verify: Some files processed (> 0) but not all (< 50)

   - `test_cancelled_documents_not_corrupted`: Document integrity check
     - Create 50 test documents
     - Calculate MD5 hash of all files before processing
     - Start processing then cancel mid-way
     - Verify: Each file either:
       a) Has original hash (not modified), OR
       b) Can be opened with python-docx without errors (successfully modified)
     - NO files should be corrupted (fail to open)

   - `test_partial_results_saved_on_cancellation`: Database state after cancel
     - Create batch and start processing
     - Cancel mid-way
     - Verify: Database has job_results for processed files
     - Verify: Processed results have correct status (success/failed)
     - Verify: Unprocessed files have no job_results entries

3. **TestCancellationTiming class:**
   - `test_cancel_before_processing_starts`: Early cancellation
     - Set up job but don't start
     - Cancel immediately after start (within 100ms)
     - Verify: Job status is cancelled
     - Verify: Very few or no files processed

   - `test_cancel_signal_from_progress_dialog`: UI-initiated cancel
     - Mock ProgressDialog.cancelled signal
     - Connect to worker.cancel
     - Emit cancelled signal
     - Verify: Worker cancellation flag set
     - Verify: Processing stops
  </action>
  <verify>Run `pytest tests/integration/test_cancellation.py -v` - tests should collect and execute</verify>
  <done>test_cancellation.py exists with TestUserCancellation and TestCancellationTiming classes</done>
</task>

<task type="auto">
  <name>Task 3: Verify all error recovery and cancellation tests pass</name>
  <files>tests/integration/test_error_recovery.py, tests/integration/test_cancellation.py</files>
  <action>
1. **Run and fix error recovery tests:**
   ```bash
   pytest tests/integration/test_error_recovery.py -v --tb=short
   ```
   - Fix any assertion failures or timing issues
   - Ensure corrupt documents trigger expected errors
   - Verify database queries return expected results

2. **Run and fix cancellation tests:**
   ```bash
   pytest tests/integration/test_cancellation.py -v --tb=short
   ```
   - Fix any timing issues with cancellation
   - Ensure worker cleanup prevents resource leaks
   - Adjust wait times if needed for reliable cancellation testing

3. **Run combined test suite:**
   ```bash
   pytest tests/integration/test_error_recovery.py tests/integration/test_cancellation.py -v
   ```
   - All tests should pass
   - No resource warnings

4. **Verify error handling edge cases:**
   - Confirm corrupt files genuinely fail (not silently skipped)
   - Confirm cancellation is responsive (doesn't take > 5 seconds)
   - Confirm database state is consistent after errors/cancellation
  </action>
  <verify>Run `pytest tests/integration/test_error_recovery.py tests/integration/test_cancellation.py -v` - all tests pass</verify>
  <done>Error recovery and cancellation tests pass, handle edge cases correctly, no resource leaks</done>
</task>

</tasks>

<verification>
Run the complete error handling test suite:
```bash
pytest tests/integration/test_error_recovery.py tests/integration/test_cancellation.py -v --tb=short
```

Expected outcomes:
- All error recovery tests pass
- All cancellation tests pass
- Corrupt documents trigger failures but don't crash
- Valid documents succeed despite failures in batch
- Cancellation is responsive and doesn't corrupt data
- Database state is consistent after errors/cancellation
</verification>

<success_criteria>
1. `tests/integration/test_error_recovery.py` exists with partial failure tests
2. `tests/integration/test_cancellation.py` exists with cancellation safety tests
3. Tests use real corrupt documents (not mocked exceptions)
4. Tests verify corrupt files are NOT modified
5. Tests verify valid files succeed despite failures in batch
6. Tests verify cancellation doesn't corrupt documents
7. Tests verify database records failure details
8. All tests pass when run: `pytest tests/integration/test_error_recovery.py tests/integration/test_cancellation.py -v`
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-e2e-tests/05-02-SUMMARY.md`
</output>
