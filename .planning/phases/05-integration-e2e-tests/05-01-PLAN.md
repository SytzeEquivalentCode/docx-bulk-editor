---
phase: 05-integration-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_e2e_workflows.py
  - tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "Full search/replace workflow completes successfully from UI to file changes"
    - "Full metadata workflow completes successfully from UI to file changes"
    - "Full validation workflow completes successfully from UI to database results"
    - "Database records job status transitions (pending -> running -> completed)"
    - "Progress signals emitted during processing"
  artifacts:
    - path: "tests/integration/test_e2e_workflows.py"
      provides: "E2E workflow tests for all 5 processors"
      min_lines: 200
  key_links:
    - from: "tests/integration/test_e2e_workflows.py"
      to: "src/ui/main_window.py"
      via: "MainWindow class instantiation"
      pattern: "MainWindow\\("
    - from: "tests/integration/test_e2e_workflows.py"
      to: "src/workers/job_worker.py"
      via: "job_completed signal wait"
      pattern: "waitSignal.*job_completed"
---

<objective>
Create end-to-end workflow tests that validate complete user journeys from file selection through processing to completion.

Purpose: Validate E2E-01 requirement - full job workflow from file selection through processing to completion works correctly. These tests verify that all components (UI, workers, processors, database) integrate correctly for each operation type.

Output: `tests/integration/test_e2e_workflows.py` with comprehensive E2E tests covering all 5 processors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-e2e-tests/05-CONTEXT.md
@.planning/phases/05-integration-e2e-tests/05-RESEARCH.md
@tests/conftest.py
@tests/gui/test_main_window.py
@src/ui/main_window.py
@src/workers/job_worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E workflow test file with test infrastructure</name>
  <files>tests/integration/test_e2e_workflows.py</files>
  <action>
Create `tests/integration/test_e2e_workflows.py` with the following structure:

1. **Imports and setup:**
   - Import pytest, PySide6 (Qt, QTest, QApplication)
   - Import MainWindow, DatabaseManager, ConfigManager, BackupManager
   - Import docx.Document for file verification
   - Apply `@pytest.mark.integration` and `@pytest.mark.timeout(60)` markers

2. **Helper fixture for E2E tests:**
   Add a fixture `e2e_window` that:
   - Creates a MainWindow with test_config and test_db
   - Registers widget with qtbot
   - Yields the window
   - Cleans up worker on teardown (cancel + wait)

3. **TestSearchReplaceE2E class:**
   - `test_full_search_replace_workflow`: Complete workflow test
     - Create 10 test documents with `docx_factory` containing "old_value" text
     - Set MainWindow.selected_files to these documents
     - Configure search_input="old_value", replace_input="new_value"
     - Use `qtbot.waitSignal(window.worker.job_completed, timeout=30000)` to wait for completion
     - Verify: All 10 files contain "new_value" and NOT "old_value" (read with python-docx)
     - Verify: Database has job record with status='completed' and success_count=10
     - Verify: Database has 10 job_results records with status='success'

4. **TestMetadataE2E class:**
   - `test_full_metadata_update_workflow`: Complete metadata workflow
     - Create 10 test documents with author="Original Author"
     - Switch to metadata operation, set author to "New Author"
     - Start processing and wait for job_completed signal
     - Verify: All documents have author="New Author"
     - Verify: Database records show completed job

5. **TestValidationE2E class:**
   - `test_full_validation_workflow`: Complete validation workflow
     - Create documents with validation issues (missing heading levels, multiple empty paragraphs)
     - Switch to validate operation with all checks enabled
     - Start processing and wait for completion
     - Verify: Job completed (validation doesn't modify files)
     - Verify: Database has validation results recorded

Key patterns to follow (from RESEARCH.md):
- Use `monkeypatch.setattr` for QFileDialog and QMessageBox (existing pattern from test_main_window.py)
- Mock ProgressDialog to avoid display issues
- Use `with qtbot.waitSignal(signal, timeout=30000)` for async completion
- Clean up workers in test teardown to prevent resource leaks
  </action>
  <verify>Run `pytest tests/integration/test_e2e_workflows.py -v` - tests should collect and run (may not all pass yet)</verify>
  <done>test_e2e_workflows.py exists with TestSearchReplaceE2E, TestMetadataE2E, and TestValidationE2E classes</done>
</task>

<task type="auto">
  <name>Task 2: Add table format and style enforce E2E tests</name>
  <files>tests/integration/test_e2e_workflows.py</files>
  <action>
Add remaining processor E2E tests to `tests/integration/test_e2e_workflows.py`:

1. **TestTableFormatE2E class:**
   - `test_full_table_format_workflow`: Complete table formatting workflow
     - Create 10 test documents with tables (use `docx_with_table` fixture)
     - Switch to table_format operation with border and header options enabled
     - Start processing and wait for completion
     - Verify: Job completed successfully
     - Verify: Database records show completion

2. **TestStyleEnforceE2E class:**
   - `test_full_style_enforce_workflow`: Complete style enforcement workflow
     - Create 10 test documents
     - Switch to style_enforce operation with font standardization enabled
     - Start processing and wait for completion
     - Verify: Job completed successfully
     - Verify: Database records show completion

3. **TestProgressSignals class:**
   - `test_progress_updates_emitted`: Verify progress signals during processing
     - Create 15 test documents
     - Connect to progress_updated signal with a collector
     - Start processing and wait for completion
     - Verify: Received progress updates with increasing percentages
     - Verify: Final progress is 100%

4. **TestDatabaseStateTransitions class:**
   - `test_job_status_transitions`: Verify database job status changes correctly
     - Create test documents
     - Query job status before processing (should be 'pending')
     - Start processing
     - Query job status after completion (should be 'completed')
     - Verify status transition: pending -> running -> completed
  </action>
  <verify>Run `pytest tests/integration/test_e2e_workflows.py -v` - all test classes should be present and most tests pass</verify>
  <done>test_e2e_workflows.py includes all 5 processor E2E tests plus progress signal and database state transition tests</done>
</task>

<task type="auto">
  <name>Task 3: Add e2e_workspace fixture and verify tests pass</name>
  <files>tests/conftest.py, tests/integration/test_e2e_workflows.py</files>
  <action>
1. **Update conftest.py** with E2E-specific fixtures if needed:
   - Add `@pytest.fixture` for `e2e_workspace` that creates complete isolated workspace
     - Creates temp_workspace structure (data/, backups/, logs/)
     - Initializes ConfigManager with correct backup directory path
     - Returns dict with {workspace: Path, config: ConfigManager, db: DatabaseManager}
   - Only add if existing fixtures don't cover the E2E test needs

2. **Verify and fix test issues:**
   - Run full test suite: `pytest tests/integration/test_e2e_workflows.py -v --tb=short`
   - Fix any import errors or fixture issues
   - Ensure worker cleanup happens in teardown to prevent resource warnings
   - Add proper signal/slot disconnect in teardown if needed

3. **Add integration marker to pytest.ini if not present:**
   - Ensure `markers = integration: integration tests` exists

4. **Verify tests pass:**
   - All E2E workflow tests should pass
   - No warnings about unclosed resources
   - Database queries verify expected state
  </action>
  <verify>Run `pytest tests/integration/test_e2e_workflows.py -v` - all tests pass, no resource warnings</verify>
  <done>E2E workflow tests pass, fixtures support complete isolated testing, proper cleanup in place</done>
</task>

</tasks>

<verification>
Run the complete E2E test suite:
```bash
pytest tests/integration/test_e2e_workflows.py -v --tb=short
```

Expected outcomes:
- All E2E workflow tests pass
- Each processor type has at least one complete workflow test
- Database state transitions are verified
- Progress signals are verified
- No resource warnings or cleanup issues
</verification>

<success_criteria>
1. `tests/integration/test_e2e_workflows.py` exists with comprehensive E2E tests
2. Tests cover all 5 processors (search_replace, metadata, validate, table_format, style_enforce)
3. Tests verify both file changes AND database records
4. Tests use qtbot.waitSignal() for async completion (not time.sleep)
5. All tests pass when run: `pytest tests/integration/test_e2e_workflows.py -v`
6. Progress signal emission is tested
7. Database job status transitions are verified
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-e2e-tests/05-01-SUMMARY.md`
</output>
