---
phase: 05-integration-e2e-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_backup_integration.py
autonomous: true

must_haves:
  truths:
    - "Backup created before document processing"
    - "Backup file is valid DOCX (can be opened with python-docx)"
    - "Backup can restore original file content"
    - "Backup recorded in database with correct paths"
    - "Restoration after simulated failure recovers original content"
  artifacts:
    - path: "tests/integration/test_backup_integration.py"
      provides: "Backup integration tests for E2E-03"
      min_lines: 150
  key_links:
    - from: "tests/integration/test_backup_integration.py"
      to: "src/core/backup.py"
      via: "BackupManager class"
      pattern: "BackupManager\\("
    - from: "tests/integration/test_backup_integration.py"
      to: "src/database/db_manager.py"
      via: "backup records query"
      pattern: "SELECT.*FROM backups"
---

<objective>
Create backup integration tests that validate backup creation before processing and restoration after failures.

Purpose: Validate E2E-03 requirement - verify backup created before processing and restorable after failure. Tests verify the backup safety mechanism works correctly to protect user data.

Output: `tests/integration/test_backup_integration.py` with comprehensive backup workflow tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-e2e-tests/05-CONTEXT.md
@.planning/phases/05-integration-e2e-tests/05-RESEARCH.md
@tests/conftest.py
@src/core/backup.py
@src/ui/main_window.py
@src/workers/job_worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backup integration test file with basic tests</name>
  <files>tests/integration/test_backup_integration.py</files>
  <action>
Create `tests/integration/test_backup_integration.py` with the following structure:

1. **Imports and setup:**
   - Import pytest, pathlib, datetime
   - Import BackupManager, DatabaseManager, Document
   - Apply `@pytest.mark.integration` class marker

2. **TestBackupCreation class:**
   - `test_backup_created_before_processing`: Core backup creation test
     - Create test document with known content: "Original content that should be preserved"
     - Initialize BackupManager with temp_workspace/backups
     - Call create_backup(doc_path, job_id="test_job")
     - Verify: Backup file exists in expected location (daily subdirectory)
     - Verify: Backup filename follows pattern: {name}.{timestamp}.backup.docx
     - Verify: Backup can be opened with python-docx (valid DOCX)

   - `test_backup_content_matches_original`: Content integrity test
     - Create document with specific content and metadata
     - Create backup
     - Open backup with python-docx
     - Verify: All paragraphs match original
     - Verify: Metadata (author, title) matches original

   - `test_backup_recorded_in_database`: Database tracking test
     - Create backup
     - Query backups table
     - Verify: Record exists with correct original_path
     - Verify: Record has correct backup_path
     - Verify: Record has job_id if provided
     - Verify: size_bytes is populated and correct

3. **TestBackupRestoration class:**
   - `test_restore_backup_to_original_location`: Basic restoration test
     - Create document and backup
     - Modify original document (change content)
     - Verify original is now different
     - Call restore_backup(backup_path)
     - Verify: Original location has restored content
     - Verify: Restored content matches original

   - `test_restore_backup_recovers_metadata`: Metadata restoration test
     - Create document with author="Original Author", title="Original Title"
     - Create backup
     - Modify document metadata
     - Restore from backup
     - Verify: author and title restored to original values
  </action>
  <verify>Run `pytest tests/integration/test_backup_integration.py -v --tb=short` - tests should collect and run</verify>
  <done>test_backup_integration.py exists with TestBackupCreation and TestBackupRestoration classes</done>
</task>

<task type="auto">
  <name>Task 2: Add full workflow backup tests with failure simulation</name>
  <files>tests/integration/test_backup_integration.py</files>
  <action>
Add additional backup tests to `tests/integration/test_backup_integration.py`:

1. **TestBackupWithProcessing class:**
   - `test_backup_created_during_job_worker_execution`: E2E backup test
     - Create test document
     - Configure MainWindow and start search_replace job
     - Wait for job completion
     - Verify: Backup exists in backup directory
     - Verify: Backup recorded in database with correct job_id

   - `test_multiple_files_all_get_backups`: Batch backup test
     - Create 10 test documents
     - Process all with MainWindow
     - Verify: 10 backup files exist
     - Verify: Database has 10 backup records

2. **TestBackupRecoveryScenarios class:**
   - `test_restore_after_simulated_modification_failure`: Failure recovery test
     - Create document with known content
     - Create backup
     - Simulate processing that "fails" after modifying file:
       a) Modify file content to "CORRUPTED: this should not remain"
       b) Do NOT save properly (simulate crash scenario)
     - Restore from backup
     - Verify: Original content restored, no corruption text

   - `test_restore_to_different_location`: Alternative restore path
     - Create document and backup
     - Restore to different target path
     - Verify: New location has restored content
     - Verify: Original file unchanged (if not overwritten)

3. **TestDatabaseBackupRecords class:**
   - `test_backup_record_includes_all_fields`: Field completeness test
     - Create backup
     - Query backup record
     - Verify fields present and valid:
       - id: non-empty string
       - original_path: absolute path
       - backup_path: absolute path
       - created_at: ISO timestamp
       - size_bytes: > 0
       - job_id: matches provided or null
       - restored: 0 initially

   - `test_restore_updates_database_record`: Restore tracking test
     - Create backup
     - Restore backup
     - Query backup record
     - Verify: restored = 1
     - Verify: restored_at is populated with timestamp
  </action>
  <verify>Run `pytest tests/integration/test_backup_integration.py -v` - all tests should pass</verify>
  <done>test_backup_integration.py includes full workflow tests and failure simulation scenarios</done>
</task>

<task type="auto">
  <name>Task 3: Add backup cleanup tests and verify all pass</name>
  <files>tests/integration/test_backup_integration.py</files>
  <action>
1. **Add TestBackupCleanup class:**
   - `test_cleanup_removes_old_backups`: Retention policy test
     - Create BackupManager with retention_days=0 (immediate cleanup)
     - Create backup
     - Manually set backup's created_at to 2 days ago in database
     - Call cleanup_old_backups()
     - Verify: Backup file deleted from disk
     - Verify: Backup record removed from database

   - `test_cleanup_preserves_restored_backups`: Restored backup protection
     - Create backup
     - Mark as restored in database
     - Set created_at to 2 days ago
     - Call cleanup_old_backups()
     - Verify: Backup file still exists (restored backups preserved)

   - `test_backup_storage_usage_calculation`: Storage tracking test
     - Create 5 backups
     - Call get_backup_storage_usage()
     - Verify: Returns sum of all backup file sizes

2. **Run full backup test suite:**
   ```bash
   pytest tests/integration/test_backup_integration.py -v --tb=short
   ```
   - Fix any failures or timing issues
   - Ensure database queries work correctly
   - Verify file operations complete successfully

3. **Edge case verification:**
   - Confirm backup fails gracefully for non-existent files
   - Confirm restore fails gracefully for missing backups
   - Confirm cleanup handles permission errors gracefully (logs warning, continues)
  </action>
  <verify>Run `pytest tests/integration/test_backup_integration.py -v` - all tests pass, no resource warnings</verify>
  <done>Backup integration tests complete, include cleanup tests, all pass, edge cases handled</done>
</task>

</tasks>

<verification>
Run the complete backup integration test suite:
```bash
pytest tests/integration/test_backup_integration.py -v --tb=short
```

Expected outcomes:
- All backup creation tests pass
- All backup restoration tests pass
- All backup cleanup tests pass
- Database records are correctly created and updated
- File integrity is preserved through backup/restore cycle
- Cleanup respects retention policy and restored flag
</verification>

<success_criteria>
1. `tests/integration/test_backup_integration.py` exists with comprehensive backup tests
2. Tests verify backup created before processing
3. Tests verify backup content matches original
4. Tests verify restoration recovers original content and metadata
5. Tests verify database tracking of backups
6. Tests verify cleanup respects retention policy
7. Tests verify restored backups are protected from cleanup
8. All tests pass when run: `pytest tests/integration/test_backup_integration.py -v`
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-e2e-tests/05-03-SUMMARY.md`
</output>
